---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: wireguardinterfaces.networking.dn42.io
spec:
  group: networking.dn42.io
  names:
    kind: WireGuardInterface
    listKind: WireGuardInterfaceList
    plural: wireguardinterfaces
    shortNames:
    - wgi
    - wgif
    singular: wireguardinterface
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WireGuardInterface is a specification for a WireGuardInterface
          resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WireGuardInterfaceSpec is the spec for a WireGuardInterface
              resource
            properties:
              addresses:
                items:
                  properties:
                    family:
                      type: string
                    local:
                      type: string
                    noPrefixRoute:
                      type: boolean
                    peer:
                      type: string
                    prefixlen:
                      type: integer
                  required:
                  - family
                  - local
                  - noPrefixRoute
                  - peer
                  - prefixlen
                  type: object
                type: array
              container:
                properties:
                  docker:
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  netns:
                    properties:
                      pid:
                        type: integer
                    type: object
                type: object
              interfaceName:
                type: string
              listenPort:
                type: integer
              moveToContainer:
                type: boolean
              mtu:
                type: integer
              node:
                type: string
              peers:
                items:
                  properties:
                    allowedIPs:
                      items:
                        type: string
                      type: array
                    endpoint:
                      type: string
                    persistentKeepalive:
                      type: integer
                    presharedKeySecretRef:
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    publicKey:
                      type: string
                  required:
                  - publicKey
                  type: object
                type: array
              privateKey:
                description: If PrivateKey is provided, the PrivateKeySecretRef will
                  not be used.
                type: string
              privateKeySecretRef:
                properties:
                  key:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - key
                - name
                type: object
            required:
            - addresses
            - interfaceName
            - listenPort
            - moveToContainer
            - node
            - peers
            type: object
          status:
            properties:
              hostname:
                description: |2-
                   Hostname of the node where the interface is provisioned,
                  or the hostname of the host of the container in case of containerization.
                  This is used to identify the node where the interface is provisioned.
                  The Hostname is not necessarily publicly reachable, since it is most likely collected from the system's hostname.
                type: string
              mtu:
                type: integer
              netlink:
                properties:
                  addrs:
                    items:
                      properties:
                        broadcast:
                          type: string
                        family:
                          type: string
                        flags:
                          type: integer
                        label:
                          type: string
                        linkIndex:
                          type: integer
                        local:
                          type: string
                        peer:
                          type: string
                        preferedLft:
                          type: integer
                        prefixlen:
                          type: integer
                        scope:
                          type: integer
                        validLft:
                          type: integer
                      type: object
                    type: array
                  alias:
                    type: string
                  altNames:
                    items:
                      type: string
                    type: array
                  flags:
                    type: integer
                  hardwareAddr:
                    type: string
                  index:
                    type: integer
                  masterIndex:
                    type: integer
                  mtu:
                    type: integer
                  name:
                    type: string
                  parentIndex:
                    type: integer
                  rawFlags:
                    format: int32
                    type: integer
                  statistics:
                    properties:
                      rxBytes:
                        format: int64
                        type: integer
                      rxDropped:
                        format: int64
                        type: integer
                      rxErrors:
                        format: int64
                        type: integer
                      rxPackets:
                        format: int64
                        type: integer
                      txBytes:
                        format: int64
                        type: integer
                      txErrors:
                        format: int64
                        type: integer
                      txPackets:
                        format: int64
                        type: integer
                    type: object
                required:
                - index
                - mtu
                - name
                type: object
              nodename:
                description: |-
                  Node name where the interface is provisioned.
                  The node name can be overridden by the operator running on the node.
                type: string
              observedGeneration:
                description: The most recent generation observed by the controller.
                format: int64
                type: integer
              wireguard:
                description: A Device is a WireGuard device.
                properties:
                  firewall_mark:
                    description: |-
                      FirewallMark is the device's current firewall mark.

                      The firewall mark can be used in conjunction with firewall software to
                      take action on outgoing WireGuard packets.
                    type: integer
                  listen_port:
                    description: ListenPort is the device's network listening port.
                    type: integer
                  name:
                    description: Name is the name of the device.
                    type: string
                  peers:
                    description: Peers is the list of network peers associated with
                      this device.
                    items:
                      description: A PeerStatus is a WireGuard peer to a Device.
                      properties:
                        allowed_ips:
                          description: |-
                            AllowedIPs specifies which IPv4 and IPv6 addresses this peer is allowed
                            to communicate on.

                            0.0.0.0/0 indicates that all IPv4 addresses are allowed, and ::/0
                            indicates that all IPv6 addresses are allowed.
                          items:
                            type: string
                          type: array
                        endpoint:
                          description: |-
                            Endpoint is the most recent source address used for communication by
                            this Peer.
                          type: string
                        last_handshake_time:
                          description: |-
                            LastHandshakeTime indicates the most recent time a handshake was performed
                            with this peer.

                            A zero-value time.Time indicates that no handshake has taken place with
                            this peer.
                          format: int64
                          type: integer
                        persistent_keepalive_interval:
                          description: |-
                            PersistentKeepaliveInterval specifies how often an "empty" packet is sent
                            to a peer to keep a connection alive.

                            A value of 0 indicates that persistent keepalives are disabled.
                          format: int64
                          type: integer
                        preshared_key:
                          description: |-
                            PresharedKey is an optional preshared key which may be used as an
                            additional layer of security for peer communications.

                            A zero-value Key means no preshared key is configured.
                          type: string
                        protocol_version:
                          description: |-
                            ProtocolVersion specifies which version of the WireGuard protocol is used
                            for this Peer.

                            A value of 0 indicates that the most recent protocol version will be used.
                          type: integer
                        public_key:
                          description: |-
                            PublicKey is the public key of a peer, computed from its private key.

                            PublicKey is always present in a Peer.
                          type: string
                        receive_bytes:
                          description: ReceiveBytes indicates the number of bytes
                            received from this peer.
                          format: int64
                          type: integer
                        transmit_bytes:
                          description: TransmitBytes indicates the number of bytes
                            transmitted to this peer.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  private_key:
                    description: PrivateKey is the device's private key.
                    type: string
                  public_key:
                    description: PublicKey is the device's public key, computed from
                      its PrivateKey.
                    type: string
                  type:
                    description: Type specifies the underlying implementation of the
                      device.
                    type: integer
                required:
                - listen_port
                - name
                - peers
                - private_key
                - public_key
                - type
                type: object
            required:
            - hostname
            - nodename
            - observedGeneration
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
